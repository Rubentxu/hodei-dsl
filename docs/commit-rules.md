# Reglas de Commit - Hodei Pipeline DSL

## ❌ PROHIBICIONES ABSOLUTAS

### **NO incluir en commits:**

1. **❌ Atribución de AI**
   ```
   🤖 Generated with [Claude Code](https://claude.ai/code)
   ❌ Generated by AI
   ❌ Created with Claude
   ```

2. **❌ Co-autoría de AI**
   ```
   Co-Authored-By: Claude <noreply@anthropic.com>
   ❌ Co-Authored-By: AI Assistant
   ❌ Co-Authored-By: ChatGPT
   ```

3. **❌ BREAKING CHANGE innecesario**
   ```
   ❌ BREAKING CHANGE: Initial project structure
   ❌ BREAKING CHANGE: First implementation
   ```
   - Solo usar cuando hay cambios reales que rompen compatibilidad

4. **❌ Disclaimers de AI**
   ```
   ❌ This commit was assisted by AI
   ❌ Generated content - please review
   ❌ AI-assisted development
   ```

## ✅ FORMATO CORRECTO

### Conventional Commits Standard
```
<type>[optional scope]: <description>

[optional body]

[optional footer]
```

### Ejemplos Correctos ✅

#### Feature nueva
```
feat(core): add pipeline DSL basic structure

- Implement Pipeline builder with type-safe DSL
- Add ExecutionContext with environment support
- Support for stage definition and execution
- Add comprehensive BDD tests with Kotest

Closes #15
```

#### Bug fix
```
fix(steps): resolve shell command encoding issues

Fix UTF-8 encoding problems when executing shell commands
in different locales.

- Use explicit UTF-8 encoding for process streams
- Add charset detection for command output
- Include regression tests

Fixes #23
```

#### Breaking change (SOLO cuando es real)
```
feat(api)!: redesign plugin API for better type safety

Plugin interface now requires explicit type parameters for better 
compile-time validation.

Migration guide:
- Old: class MyPlugin : PipelinePlugin
- New: class MyPlugin : PipelinePlugin<Pipeline>
```

#### Documentación
```
docs(architecture): add hexagonal architecture specification

- Document core domain boundaries
- Define port and adapter patterns
- Include dependency injection guidelines
- Add testing strategy for each layer
```

#### Refactor
```
refactor(executor): extract stage execution logic

Extract complex stage execution into separate classes
following Single Responsibility Principle.

- Create StageExecutor hierarchy
- Improve code testability
- Maintain backward compatibility
```

## 🎯 PRINCIPIOS

### **El desarrollador es el único autor**
- Los commits reflejan el trabajo del desarrollador
- Las herramientas AI son parte del entorno de desarrollo
- No requieren atribución especial

### **Claridad y profesionalismo**
- Commits limpios y centrados en el cambio técnico
- Sin referencias a herramientas de desarrollo
- Enfoque en el valor del negocio y técnico

### **Conventional Commits estricto**
- Formato consistente para automatización
- Facilita generación de changelogs
- Mejora la navegación del historial

---

**ESTAS REGLAS SON OBLIGATORIAS Y NO NEGOCIABLES**